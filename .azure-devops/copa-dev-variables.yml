# Development Environment Variables
# Variable Group: copa-dev-variables

# Environment Configuration
resourceGroupName: rg-btp-d-copa-stop-search
azureLocation: uksouth
environmentName: development
environmentCode: d
instanceNumber: '001'

# OpenAI Configuration
openAIModel: gpt-4o
embeddingModel: text-embedding-3-small

# BTP Naming Convention - Web App Name
# Format: app-btp-{environmentCode}-copa-stop-search-{instanceNumber}
webAppName: app-btp-d-copa-stop-search-001

# Additional Development Settings
deploymentSlotName: staging
enableDebugMode: true

# Security Settings for Development
# 🔐 Mark these as secrets in Azure DevOps
appClientId: $(dev-app-client-id)
appClientSecret: $(dev-app-client-secret)

# Service Connection
azureServiceConnection: copa-azure-service-connection-dev